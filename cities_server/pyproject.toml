[build-system]
requires = ["setuptools>=64.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cities"
version = "0.1.0"
dependencies = [
    "bs4",
    "Flask",
    "requests",
    "spacy",
    "nltk"
]
requires-python = ">=3.8"

[tool.setuptools]
packages = ["cities"]